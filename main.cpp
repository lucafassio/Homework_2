#include <iostream>
#include "Ej1/timer.cpp"
#include "Ej2/class.cpp"

using namespace std;

int main(){
    timer_console();
    
    // Curso proba("Probabilidad y Estadistica: ");
    // estudiante tuta("Lautaro Lo Presti", 123, 8.5);
    // estudiante baury("Bautista Rocco Garcia", 321, 8.5);
    // estudiante puerta("Lorenzo Door", 213, 8.5);
    // estudiante luca("Luca Fassio", 312, 8.5);
    // proba.add_student(tuta);
    // proba.add_student(baury);
    // proba.add_student(puerta);
    // proba.add_student(luca);
    // proba.students_list();

    // proba.take_student(213);
    // proba.take_student(404);
    // proba.students_list();

    // if (proba.is_full()) cout << "El curso esta lleno." << endl;
    // else cout << "Queda lugar en el curso" << endl;

    // for (int i=0; i<17; i++){
    //     estudiante aux("Test_" + to_string(i+1), i, 8.5);
    //     proba.add_student(aux);
    // }

    // proba.students_list();
    // if (proba.is_full()) cout << "El curso esta lleno." << endl;
    // else cout << "Queda lugar en el curso" << endl;

    // estudiante no("No entra", 21, 8.5);
    // proba.add_student(no);
    
}